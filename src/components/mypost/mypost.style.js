import { styled } from "styled-components";

export const Main = styled.main`
  width: 100%;
  margin-top: 2rem;
`;
export const MyPostsWrapper = styled.ul`
  display: flex;
  flex-direction: column;
  align-items: center;

  max-width: 80rem;
  margin: 0 auto;
  font-size: 1.6rem;
  color: (--textColor);
`;
